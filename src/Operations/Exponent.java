package Operations;

public class Exponent {
    public static double exponent(String number1, String number2) {
        return Math.pow(Double.parseDouble(number1), Double.parseDouble(number2));
    }
}
